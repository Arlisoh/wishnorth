"use client";

import { FormEvent, useState } from "react";
import Header from "@/components/Header";
import Footer from "@/components/Footer";

export default function ReportPage(){const[shareUrl,setShareUrl]=useState("");const[reason,setReason]=useState("privacy");const[details,setDetails]=useState("");const[email,setEmail]=useState("");const[website,setWebsite]=useState("");const[notice,setNotice]=useState("");const[error,setError]=useState("");async function submit(e:FormEvent){e.preventDefault();setError("");setNotice("");try{const u=new URL(shareUrl);const parts=u.pathname.split("/").filter(Boolean);const index=parts.indexOf("share");const token=index>=0?parts[index+1]:"";if(!token)throw new Error("Paste a valid Wish North shared-list URL.");const res=await fetch("/api/abuse-report",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({shareToken:token,reason,details,email,website})});const data=await res.json();if(!res.ok)throw new Error(data.error||"Could not submit report");setNotice("Report received. Thank you. Wish North can now review and, if necessary, hide or remove the content.");setDetails("");}catch(e){setError(e instanceof Error?e.message:"Could not submit report");}}return <main><Header/><section className="form-shell shell-narrow"><div className="eyebrow">SAFETY</div><h1>Report a Wish North list</h1><p className="form-intro">Use this form for privacy concerns, harassment, scams, impersonation, unsafe content, or intellectual-property concerns.</p><form className="big-form" onSubmit={submit}><label><span>Shared-list URL</span><input required value={shareUrl} onChange={e=>setShareUrl(e.target.value)} placeholder="https://wishnorth.netlify.app/share/…"/></label><label><span>Reason</span><select value={reason} onChange={e=>setReason(e.target.value)}><option value="privacy">Privacy</option><option value="unsafe_or_inappropriate">Unsafe or inappropriate</option><option value="harassment">Harassment</option><option value="impersonation">Impersonation</option><option value="spam_or_scam">Spam or scam</option><option value="copyright_or_trademark">Copyright or trademark</option><option value="other">Other</option></select></label><label><span>Details</span><textarea rows={5} maxLength={2000} value={details} onChange={e=>setDetails(e.target.value)} placeholder="Tell us what we should review."/></label><label><span>Email (optional)</span><input type="email" maxLength={320} value={email} onChange={e=>setEmail(e.target.value)} placeholder="you@example.com"/></label><label className="honeypot" aria-hidden="true"><span>Website</span><input tabIndex={-1} autoComplete="off" value={website} onChange={e=>setWebsite(e.target.value)}/></label>{error?<div className="error-box">{error}</div>:null}{notice?<div className="success-box">{notice}</div>:null}<button className="button button-primary button-big full">Submit report</button></form></section><Footer/></main>;}