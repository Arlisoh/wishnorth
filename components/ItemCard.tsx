"use client";
import { money } from "@/lib/helpers";
import { productImageUrl } from "@/lib/productImage";
import type { WishItem } from "@/lib/types";
export default function ItemCard({item,publicView=false,onClaim,onDelete,onEdit,reportHref}:{item:WishItem;publicView?:boolean;onClaim?:(item:WishItem)=>void;onDelete?:(item:WishItem)=>void;onEdit?:(item:WishItem)=>void;reportHref?:string}){return <article className={`gift-card ${item.claimed?"gift-card-claimed":""}`}><div className="gift-image-wrap">{item.image_url?<img src={productImageUrl(item.image_url)} alt="" className="gift-image" loading="lazy" referrerPolicy="no-referrer"/>:<div className="gift-placeholder">🎁</div>}{item.claimed&&publicView?<div className="claimed-stamp">TAKEN</div>:null}</div><div className="gift-content"><div className="eyebrow">{item.retailer||"Wish"}</div><h3>{item.title}</h3>{item.price_cents!=null?<div className="price">{money(item.price_cents,item.currency)}</div>:null}<div className="chips">{item.size?<span>Size: {item.size}</span>:null}{item.color?<span>{item.color}</span>:null}<span>{"♥".repeat(item.priority)}</span></div>{item.notes?<p className="gift-notes">{item.notes}</p>:null}<div className="gift-actions">{item.url?<a href={item.url} target="_blank" rel="noopener noreferrer" className="text-link">View item ↗</a>:null}{publicView&&!item.claimed&&onClaim?<button className="button button-dark" onClick={()=>onClaim(item)}>I’m getting this</button>:null}{publicView&&item.claimed?<span className="taken-copy">Someone has this covered ✨</span>:null}{publicView&&reportHref?<a className="plain-link report-link" href={reportHref}>Report</a>:null}{!publicView&&onEdit?<button className="plain-link" onClick={()=>onEdit(item)}>Edit</button>:null}{!publicView&&onDelete?<button className="plain-link danger-link" onClick={()=>onDelete(item)}>Delete</button>:null}</div></div></article>}
